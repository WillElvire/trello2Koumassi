import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.trello.app',
  appName: 'trelloApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
